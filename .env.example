# -----------------------------------------
# CONFIG SELLITE PRO (FAKE PAYMENTS VERSION)
# -----------------------------------------

# URL du site (Render va remplacer automatiquement par ta vraie URL)
BASE_URL=https://ton-site.onrender.com

# Fake Keys (obligatoires pour éviter erreurs mais NON utilisées)
STRIPE_SECRET_KEY=fake_stripe_secret
STRIPE_PUBLISHABLE_KEY=fake_stripe_pub
STRIPE_WEBHOOK_SECRET=fake_stripe_webhook

COINBASE_COMMERCE_API_KEY=fake_coinbase_key

# Session secret (pour sécuriser les sessions)
SESSION_SECRET=supersecret123

# Nom de la base SQLite (dans /server)
DB_FILE=database.sqlite
